"use client";
import { Suspense } from 'react';
import InicioDevolver from '../components/pdrl/InicioDevolver';

export default function Devolver() {
    return(
        <Suspense>
            <InicioDevolver/>
        </Suspense>
    )
}